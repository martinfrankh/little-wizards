version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: echo "Building the job"
  test:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: echo "testing the job"

workflows:
  build_and_test:
    jobs:
      - build
      - test